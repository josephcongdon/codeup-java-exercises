public class JavaDrills {
    // work in progress
    public static String flipOuterCase(String word){
        for(int i = 0; i <= word.length(); i++){
            String firstLetter = String.valueOf(word.charAt(0));
            String lastLetter = String.valueOf(word.charAt(0));

            if()
        }
    }

}
